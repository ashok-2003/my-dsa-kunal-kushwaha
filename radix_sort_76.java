public class radix_sort_76 {
    // so now here we will learn about the radix sort how we can do the radix sort
    // so in radix sort we do the count sort number of time the digits place are
    // so like first of all we will do the radix sort for the first place then for the second place then for the
    // the third place digit like wise as we have to do that
    public static void main(String[] args) {
        System.out.println(Math.log(999)/Math.log(10)); // so it give 2 in int as it is 3 digit number that is with 2 zero

        // this is good sorting algo but i am not doinng it please do it later on 
    }
}
